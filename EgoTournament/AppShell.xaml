<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="EgoTournament.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:views="clr-namespace:EgoTournament.Views"
	xmlns:local="clr-namespace:EgoTournament"
    Shell.FlyoutBehavior="Flyout">
    <ShellContent
        Title="Loading"
        ContentTemplate="{DataTemplate views:LoadingPage}"
        Route="LoadingPage"
        Shell.FlyoutBehavior="Disabled"
        Shell.FlyoutItemIsVisible="False" />

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Route="LoginPage"
        Shell.FlyoutBehavior="Disabled"
        Shell.FlyoutItemIsVisible="False"/>

    <ShellContent
        Title="SignUp"
        ContentTemplate="{DataTemplate views:SignUpPage}"
        Route="SignUpPage"
        Shell.FlyoutBehavior="Disabled"
        Shell.FlyoutItemIsVisible="False"/>

    <TabBar FlyoutDisplayOptions="AsMultipleItems">

        <ShellContent
        Title="Profile"
        ContentTemplate="{DataTemplate views:ProfilePage}"
        Route="ProfilePage" />

        <ShellContent
        Title="Ego Tournament"
        ContentTemplate="{DataTemplate views:MainPage}"
        Route="MainPage" />

        <ShellContent
        Title="Search Summoner"
        ContentTemplate="{DataTemplate views:SearchSummonerPage}"
        Route="SearchSummonerPage" />
        
    </TabBar>


</Shell>